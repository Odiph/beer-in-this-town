---
name: Bug report
about: Something behaved incorrectly
labels: bug
---

**What happened**

**What you expected**

**Command you ran**
```
python -m untappd_maps ... --json
```

**The JSON envelope** (if you have it — it carries the error code and remedy)
```json
```

**Environment**
- OS:
- Python version:
- Installed via: `pip install -e .` / other

> If a parse failed, `debug/` will contain the page HTML. Those dumps were
> fetched with your logged-in session and can include your username — please
> quote only the relevant fragment rather than pasting the whole file.
