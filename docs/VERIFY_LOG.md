# /verify-change run record

One line per run. The failure-mode table in the skill stays evidence-backed
only while it is still being fed; when a row stops earning its place, delete
it from both.

| Date | Mode | Commit | Steps | Caught |
|---|---|---|---|---|
| 2026-09-20 | full | `aa2f386` | 0–10 | Step 7: `SCHEMA_VERSION` policy was nowhere stated while five envelope fields had been added; `version` still `0.1.0` after 14 behaviour-changing commits. Step 10: subject-line-only grep reported "no issues referenced" — a false finding; bodies close #10, #12, #13, #14, #15. Steps 0–6 clean. |
| 2026-09-20 | full | `4708d14`+wip | 0–10 | Step 1: `PlacesUnavailable` subclasses `RuntimeError` and both call sites caught it, but an escape would have landed on `unexpected_error` whose remedy is "re-run" — wrong for a dead key; mapped handler added in `main`. Step 3: the new cache crashed the run on a corrupt read, and was only written after the loop, so a key dying halfway threw away lookups already billed — both fixed, both tested. Mutation-tested the pre-flight and the handler: removing either fails exactly the test written for it. Steps 0, 2, 4–10 clean. |
| 2026-09-20 | full | `fb0f26c`+ui | 0–10 | Step 1: `verify_*` separated "could not run" from "signed out", and `_google_check` collapsed them back into "sign in again" — wrong advice when Playwright is missing; `VerifyResult.ran` now carries the distinction and two tests hold it in both directions. Also found by socket test: a refused POST replied without draining its body, so the client saw a connection reset instead of the 403 (a correct guard reading to the user as a crash), and the first fix for it then double-drained and hung. Step 4: `ui` is blocking and human-only, kept out of `next_actions` and documented as such in AGENTS.md. Step 8: new listening socket — four guards, all tested over a real socket, plus the absence of any pin/notes route. Steps 0, 2, 3, 5–7, 9–10 clean. |
| 2026-09-20 | full | `dca1c8c`+fixes | 0–10 | **Step 4 found the session bug the whole UI was built to prevent, still live in the agent contract**: `blocked_on` read `logged_in` (a file exists), so on this machine — both accounts verified dead — it reported `null` and offered `run`, which signed out of Untappd yields a five-venue corpus reported as a finished scrape. `verify` now gates `run`, with a 12h-expiring record so the loop still terminates. Hints were keyed off the same file, so the expired case named the right error code and then talked about ledger locks. Step 1: `cmd_ui` caught only RuntimeError from `serve_detached`, but Popen raises OSError — a process that will not start was answered with "re-run with -v". Step 3: the new `state/ui.json` was written non-atomically while a parent polled it. Step 7: `port_unavailable` was missing from the error table. Step 8: `ui --detach` writes the dashboard key to disk — now disclosed in SECURITY.md. All five fixes mutation-tested. Suite 3.9s → 11.8s this session, traced to socket-fixture teardown, not chrome-profile. Steps 2, 5, 6, 9, 10 clean. |
| 2026-09-20 | quick | `86d64d3`+overpass | 0–6 | **Step 1 is a landmine rather than a pass.** `OverpassUnavailable` subclasses `RuntimeError`, so every `except RuntimeError` in `cli.py` (1044, 1139) and `http_client.py` (389) would eat it — it survives today only because nothing calls the module yet, which is the weakest possible reason. Whoever wires it into `run` must add an `overpass_unavailable` code to the envelope and AGENTS.md and order its handler before any `except RuntimeError`; unmapped it lands on `unexpected_error`, remedy "re-run", which is wrong for a bad `OVERPASS_URL`. **The live run earned a fix**: the first real call returned 258 venues and the second 504'd, because `nearby_venue_names` re-fetched what `nearby_venues` had just read — one answer was costing two requests to a volunteer endpoint whose usage policy asks for caching. Now cached on disk, keyed on endpoint+query, and a failure is never cached (a 504 stored as "no bars here" would outlive the outage by 12h). Cache isolated per test via tmp_path, or one test's answer serves another's. Steps 0, 2–6 clean; 482 tests, ruff clean; `state/` unescaped. |
| 2026-09-21 | quick | `caab9a4`+sweep | 0–6 | **Step 1 landmine, same shape as `OverpassUnavailable`.** `WrongScreen` and `DeadPan` both subclass `RuntimeError`, so every `except RuntimeError` in `cli.py` (1044, 1139) and `http_client.py` (389) would eat them; they survive only because nothing calls the modules yet. Wiring needs envelope codes and handlers ordered first. **Step 2 caught a real design error in my own tests**: I asserted a truncated cell's 58 venues should be discarded in favour of its children's. Wrong — truncated means incomplete, not wrong, and throwing them away loses real venues; the module was right and the test was fixed. Also corrected an assertion that `Refresh search` fires 5 times for 5 cells: it fires 4, once per child, because the parent's result set comes from the caller's own search. **Validated against the four real dumps** captured today — Tel Aviv 58 truncated, Carmel 58 truncated, Haifa 37, zoomed 29, list view correctly refused by the guard. Steps 0, 3–6 clean; 511 tests, ruff clean, `state/` unescaped. |
| 2026-09-21 | quick | `41bb973`+driver | 0–6 | **The live run earned three fixes that no unit test would have found.** (1) `is_truncated` tests the *viewport* while the cap is a property of the *result set*: a map left panned showed 56 of a 58-venue set, the sweep called the cell complete and declined to subdivide. Every cell now re-searches before it harvests, which puts the result set inside the viewport by construction; the test asserting 4 refreshes for 5 cells was me encoding the bug and now asserts 5. (2) `DeadPan` fired for real: a pan of (225,331) px moved the map (-2,0) because the screen centre had a marker on it and dragging a pin pans nothing. Pans now start from the point furthest from every pin. Without that guard the cell would have silently re-harvested the same rectangle while reporting progress. (3) No settle time after gestures — a dump taken mid-redraw returns fewer pins, which the cap rule reads as 'complete'; jittered 5-8s settles added, floor documented. Also: `RESULT_SET_CAP` is a threshold, not a ceiling — cells returned 59 and 60. **End-to-end validated**: 5 cells, 93 venues vs 58 from one search, 35 new including Milk & Honey Distillery, Beer Point, Crafters & Co, Teder.fm. Steps 0, 3–6 clean; 522 tests, ruff clean. |
| 2026-09-21 | quick | `b1b0ce2`+wiring | 0–6 | **Step 1 landmine closed.** `OverpassUnavailable`, `AdbUnavailable`, `DeadPan`, `CategoryPanelError` and `WrongScreen` all subclass `RuntimeError`; each now has its own clause in `main` ahead of the `except Exception` fall-through, which would otherwise have answered 're-run with -v' — wrong advice for all five. Four new codes documented in AGENTS.md. **Step 3**: the sweep journal is new persisted state — scoped per city like `pinned_<list>.json` (an unscoped one would let a Haifa sweep resume into a Tel Aviv corpus), written after every cell rather than at the end, and an unreadable journal starts fresh instead of crashing. **Also found**: `ensure_map_screen` had hardcoded 6-8s settles, so its first test slept 32s; parameterised. Navigation now asserts the map before typing — a live run had typed a city name into a venue page because the guard protected the harvest and not the navigation. Steps 0, 2, 4–6 clean; 572 tests, ruff clean. |
| 2026-09-21 | quick | `62b799f`+camera,calibrate | 0–6 | **Step 1: `CalibrationFailed` is the next landmine, not yet live.** It subclasses `RuntimeError` and has no caller in the package, so nothing eats it today; wiring calibration into a command needs an envelope code and a clause ahead of `except Exception`. The new `WrongScreen` (relaunch found no `View Map`) is raised after the polling loop, outside both `except WrongScreen` blocks, and reaches `app_screen_unexpected`; `location()` raises `AdbUnavailable` itself, outside `_adb`'s catch-all. **The live run earned four fixes**: swept venues carried per-viewport pixels, meaningless after any pan (now `Camera` converts at harvest and follows every pan); `launch` resumed a crashed sweep's filter panel and recovery tapped a remembered coordinate into it (now force-stop, locate `View Map`, poll a cold start); a `Killed` uiautomator ended a depth-3 run (one retry); and the assumed 10.2 m/px was 8.12 after `Reset location`, putting every venue ~600 m off in one direction. **Step 2 caught a real weakness**: least squares let one 4 km mismatch drag the fit to a 1 km median, hiding the outlier; consensus-then-refit fixed it. Independent check against Untappd pages: Schnitt 599 m -> 18 m, Lauter 627 m -> 120 m (its known marker nudge). Steps 0, 3-6 clean; old pixel-only journals load with coordinates `None`; 632 tests, ruff clean. |
| 2026-09-22 | full | `105faba` | 0–14 (10 and 13's `gh` calls skipped: offline) | **Step 1: `StatsLoginRequired` subclasses `ParseError`, so `selfcheck`'s `except ParseError` (cli.py:476) reports a signed-out session as `selectors_stale` and points at a debug dump that was never written.** Step 3: `enrich` runs two `ReadBudget` instances (client + `BrowserNameSearch`) that each keep their own in-memory count and overwrite each other's file, so the hourly ceiling under-counts. Step 11: sdist is allowlisted and clean (104 entries); one stray untracked `??` file at the root; gitleaks clean over 127 commits. Also open: SECURITY.md omits Overpass and adb; actions still on `@v7` tags; the adb fix in tests (Step 14). 640 unit tests, ruff clean, suite ~28s (UI-server teardown ~0.5s each). **All fixed before merge**: selfcheck maps the gate to `not_signed_in`; `ReadBudget` re-reads its file on every read and record, and enrich shares one; stop re-raises log first; SECURITY.md names Overpass and adb; actions pinned to SHAs; the adb fix replaced with a public landmark; AGENTS.md and SECURITY.md added to the sdist; stray file removed. 833 tests. |
| 2026-09-23 | quick | `039aece`+search-method (uncommitted) | 0–6 | **Step 1: an unreadable search page raised a bare `RuntimeError`**, which reached the user as `unexpected_error` ('re-run with -v'); now `SearchPageUnreadable` -> `search_unavailable`, documented in AGENTS.md. **Review caught**: the search sweep recorded no method, so a dashboard `map` choice silently outranked the search that had just run; the test that should have caught it stubbed `record_run`. **Live run caught**: `--json` crashed on a Windows console printing a Hebrew variant (cp1252); envelopes are now ASCII-escaped. Steps 0, 2-6 clean; 904 tests, ruff clean. |
| 2026-09-23 | quick | `039aece`+search-method, review pass (uncommitted) | 0–6 | **Found in review: a stalled "Show More" was read as the end of the results.** `BrowserCitySearch._load` stopped quietly when a click loaded nothing in 15 s, cached the short page for `cache_ttl_s`, and `harvest` warned that "the rest are less-visited venues" -- the same silent gap the Tel Aviv probe hit (440 of 465, and four letters read as 0). Now one retry, then `complete: false` per variant with its own warning, and an incomplete page is never cached, so the re-run the warning asks for retries it. Tests at the harvest and cache layers. **Also**: the dashboard sent a search user to BlueStacks setup; the emulator step now blocks only a `map` choice, the city step asks for the method, and the Build card's sweep row follows it. Step 1: `SearchPageUnreadable` reaches `search_unavailable`, `BudgetExceeded` from a search page reaches `rate_limited`; the CLI's `except RuntimeError` clauses are `pin`/`notes` only. Steps 0, 2-6 clean; 915 tests, ruff clean. Noted, not caused here: `test_only_one_job_runs_at_a_time` sometimes prints "--- Logging error ---" when a job thread logs after capture closes. |
| 2026-09-24 | full | feat/search-method (`81fdf1b`) | 0–14 (13's `gh api` calls skipped) | **Step 8 caught a missing host**: `sweep --method search` sends the city name to Nominatim for its boundary (whatever geocoder is configured) and reads byte ranges from `data.source.coop`; neither was in SECURITY.md. Added, with what each receives. **Step 12**: README still said the app map was the only way to collect venues; now describes the search method beside the default map. Default switched back to `map` (the user's call, 2026-09-24): status, the dashboard's pre-selected option and every doc follow it. sdist 118 entries, gitleaks clean, actions SHA-pinned, only a business's coordinates in the new tests. 917 tests, ruff clean. |
| 2026-09-24 | full | fix/notes-editor (`bbff087`+docs) | 0–14 (13's `gh api` calls skipped) | **Found live, three layers deep.** Maps moved the note under the folded "Saved in" row, one box per list; `notes` timed out, and would have typed into the first box (The Rake's other list is shared). The first fix read `\s+` where innerText has "Saved inLondon Bars Test"; the second targeted the unfolded row's button, so the reload that verifies a note folded it again and a landed note counted as the third failure -- the breaker tripped (6h cool-off, honoured). Step 1: the new `NoteBoxMissing` is caught by the loop's `except Exception` on purpose (a per-place failure); `_read_note`'s catch-all now logs instead of swallowing, which is what hid the second bug. Step 2: the box choice, the folded toggle and the no-box decision are tested at the functions the loop calls; the envelope at `cmd_notes`. Also: `notes` adopted a Singapore `noted.json` by guessing; restored, and no longer adopted. sdist 113 entries, gitleaks clean, actions SHA-pinned, no personal data in the diff. 893 tests, ruff clean. |
