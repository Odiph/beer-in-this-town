"""beer-in-this-town: a craft-beer map of a city, in a Google Maps saved list."""

# The single source of the version. `pyproject.toml` reads it (hatch dynamic
# version) and every User-Agent this tool sends to a third party reads it, so
# they can no longer disagree the way `1.0` and `0.1.0` once did.
__version__ = "0.4.0"

PROJECT_URL = "https://github.com/Odiph/beer-in-this-town"
